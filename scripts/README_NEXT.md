# Next validation

This file marks the current automation checkpoint. The FANZA matcher now aggregates all API candidates and only auto-matches with strong identity signals (JAN, exact product code, title+performer/date combinations). Future workflow runs should be inspected for match counts before further matcher changes.
